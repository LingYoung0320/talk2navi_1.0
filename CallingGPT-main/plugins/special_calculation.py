def special_calculation(a: int, b: int) -> int:
    """Calculate two numbers by this formula: 2*a+b-1.

    Args:
        a: The first number.
        b: The second number.

    Returns:
        a special calculation on two numbers
    """
    return 2 * a + b - 1
